apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-quest.fullname" . }}-vote
  labels:
    app: vote
spec:
  type: {{ .Values.vote.service.type }}
  ports:
    - port: {{ .Values.vote.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vote
