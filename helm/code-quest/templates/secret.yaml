{{- if not .Values.externalSecrets.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "code-quest.fullname" . }}-secret
  annotations:
    note: "For production, use External Secrets Operator to sync this from GCP Secret Manager"
type: Opaque
data:
  # The password "postgres" encoded in base64
  POSTGRES_PASSWORD: cG9zdGdyZXM=
{{- end }}
